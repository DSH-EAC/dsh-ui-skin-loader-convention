# DSH UI 皮肤加载公约（UI Skin Loader Covenant）v1

> 这是一份**弱约束**公约：它只定义"皮肤插件"与"加载器"之间的最小接缝——皮肤不占用什么、何时被启用、收到什么、何时必须彻底关闭。除此之外的一切，皮肤插件完全自己实现。
> 本公约不是包格式、不是安装器、不是管理器实现、不是宿主 Profile。

| 字段 | 值 |
| --- | --- |
| 协议 id | `dsh.ecosystem.ui-skin-loader/v1` |
| 类别 | sub-protocol（buildsOn: dsh-std） |
| 状态 | Experimental（状态词照录：Draft / Experimental / Candidate / Stable / Deprecated） |
| 维护者 | DSH-EAC |
| 适用宿主 | `deepseek-ai/deepseek-harness`，首个适配基线 `0.1.7-rc.2`（经加载器 adapter 适配，宿主版本不是公约的约束轴） |
| 公约版本轴 | `dsh.ecosystem.ui-skin-loader/v{N}`，语义破坏升 major；新增可选字段不升版本 |

## 1 它是什么

一张"皮"（UI 皮肤插件）与一个"加载器"之间的最小契约：

- 任何遵循本公约的皮肤插件，可以被任何遵循本公约的加载器**发现、展示、激活、彻底关闭**；
- 任何遵循本公约的加载器，可以安全地同时面对多个皮肤插件，而**不用担心未激活的皮肤占用任何东西**。

公约的全部强制内容只有三节：§3 唯一声明、§4 生命周期、§5 不占用清单。§6 槽位是纯可选的新增机制。

## 2 角色与边界

| 角色 | 定义 | 公约约束谁 |
| --- | --- | --- |
| Skin（皮肤插件） | 一个声明了本公约的标准 DSH 插件，提供一套可整体启停的视觉效果 | 受约束（§3/§4/§5） |
| Loader（加载器） | 发现皮肤、呈现控制台、裁决唯一激活皮肤、下发启停语义的插件 | 受约束（§4 的加载器侧义务） |
| Host（宿主） | DSH 运行时本身（Cordis 插件树） | 不受约束 |

本公约不规定 Loader 如何实现（控制台长什么样、如何持久化、用不用故障隔离），只规定它对 Skin 的启停语义必须满足 §4。

## 3 唯一强制声明（发现）

皮肤插件在其 `package.json` 中声明一个字段。这是公约对皮肤要求的**唯一"占用"**：

```json
{
  "dsh": {
    "skin": {
      "apiVersion": "dsh.ecosystem.ui-skin-loader/v1",
      "id": "example.aurora",
      "name": "极光之夜",
      "version": "1.0.0"
    }
  }
}
```

| 字段 | 规则 |
| --- | --- |
| `apiVersion` | 精确匹配本公约版本；加载器遇未知 major 版本按"不兼容"展示，不激活 |
| `id` | 小写反域名，`[a-z0-9]+(?:[.-][a-z0-9]+)*`；同一皮肤血统内不可变 |
| `name` / `version` | 展示名与 SemVer，仅为展示与诊断，不是身份 |
| 可选字段 | `author`、`description`、`tags`、`preview`（内联 SVG 或相对路径）、`slots`（声明自愿贡献的扩展槽位，见 §6）。加载器必须忽略一切未知字段 |

**声明不等于激活。** 一个已安装、已声明、甚至 fiber 已存活的皮肤，在收到 §4 的激活语义之前，必须处于"零可见副作用"状态（红线 R1）。

## 4 生命周期：何时启用、收到什么、何时彻底关闭

### 4.1 状态机

```
installed（已安装，加载器可见或不可见）
   → discovered（加载器已发现并完成登记；未激活；零副作用）
   → active（被用户显式选中；唯一）
active → discovered（被换走或被停用；彻底关闭后回到此态）
```

**全局互斥**：同一宿主实例内，任意时刻至多一个皮肤处于 `active`。互斥由加载器单点裁决，皮肤无权自行进入 active（R6）。

### 4.2 两个语义事件

公约只定义两个语义事件，**不规定传输机制**（推荐实现：皮肤的 client 半向加载器服务登记 `activate/deactivate` 回调；或加载器经宿主标准插件启停驱动皮肤 fiber，Cordis dispose 自动逆序释放 `ctx.effect`）：

| 事件 | 载荷 | 皮肤的义务 |
| --- | --- | --- |
| `skin/activate` | `SkinContext`：加载器句柄、结构化 logger、中止信号、（可选）扩展槽位句柄 | 自此才允许产生可见副作用；副作用必须可逆 |
| `skin/deactivate` | 无 | **彻底关闭**：撤销激活以来产生的全部副作用 |

皮肤的自定义设置（换背景图、调透明度等）**不是公约事件**，由皮肤自行实现；加载器不得代为托管，也不得因皮肤提供/不提供设置而区别对待。

### 4.3 彻底关闭的五种触发与判定

触发（加载器必须下发 `skin/deactivate` 或等效 fiber dispose）：

1. 用户切换到另一皮肤；
2. 用户手动停用当前皮肤（回到默认观感）；
3. 加载器自身被停用/卸载；
4. 宿主退出；
5. 加载器判定该皮肤故障（激活抛错/健康超时）后的隔离动作。

判定标准（一句话）：**退出后不可观测**——DOM 节点、样式表、主题覆盖、槽位注册、事件监听、定时器、动画帧、worker、网络连接全部撤销，宿主观感与该皮肤从未激活时逐像素一致。

### 4.4 加载器侧义务

- 发现全部已声明皮肤并展示，**默认不激活任何皮肤**；
- 激活新皮肤前必须先完成对旧皮肤的 `skin/deactivate`（或等效 dispose）；
- 激活状态的唯一事实来源是加载器；加载器负责跨重启恢复用户选择；
- 皮肤 `activate` 失败：回到无皮肤状态并展示错误，不得残留半个新皮肤；
- 皮肤 `deactivate` 超时/失败：标记该皮肤"疑似残留"，如实告知用户，不得谎报成功。

## 5 不占用清单（红线）

| # | 红线 |
| --- | --- |
| R1 | **未激活零副作用**：未收到 `skin/activate` 前，不注入 DOM/CSS/主题覆盖，不注册壳级槽位内容，不挂全局监听/定时器；唯一允许的行为是向加载器登记自身元数据 |
| R2 | **不占用加载器保留面**：加载器控制台 UI；保留 service 名 `uiSkinLoader`；保留 settings 命名空间 `dsh-ui-skin-loader`；保留槽位 id 前缀 `io.github.dsh-eac.skin.loader.*` |
| R3 | **不触碰其他皮肤**：不得读写其他皮肤的挂载根、样式、资源与状态 |
| R4 | **不触碰宿主恢复面**：不得样式化/移除宿主崩溃页、诊断页、回退观感与窗口控制件 |
| R5 | **不依赖非 ABI**：CSS-module hash、上游私有 DOM/类名、源码路径、HMR 内部均不是 ABI；依赖即违规 |
| R6 | **不自行持久激活**：不得自启、不得自我恢复、不得绕过加载器记住"上次是我"；激活状态的唯一事实来源是加载器 |
| R7 | **不启停他人**：不得启用/禁用/改写任何其他插件（包括读写宿主插件清单与 patch 配置） |
| R8 | **退出即净**：满足 §4.3 判定；推荐把全部副作用登记进 `ctx.effect`，由 fiber dispose 自动逆序释放 |

违反任一条：加载器可将该皮肤标记为不兼容并拒绝激活；公约层面没有其它处罚——本公约是接缝，不是监管。

## 6 扩展槽位（可选，只新增）

加载器（或任何宿主侧插件）可以向皮肤暴露**扩展槽位**，例如"上拉栏/顶栏扩展区"。规则只有三条：

1. **只新增**：槽位集只增、不改、不删；弃用仅在登记表中标记 `Deprecated`，永不复用该 id；
2. **自描述**：每个槽位登记 `id`（反域名）、`kind`（single/list/keyed/chain）、`scope`（root/session）、fallback 观感；
3. **可忽略**：皮肤可以只声明 `dsh.skin` 而不贡献任何槽位；也可以忽略自己不认识的槽位。加载器不得因皮肤未贡献某槽位而报错。

初始槽位表（Experimental，随公约演进只做新增）：

| 槽位 id | kind | scope | 说明 |
| --- | --- | --- | --- |
| `io.github.dsh-eac.skin.slot.topbar-extension` | list | root | 顶栏/上拉栏扩展区；皮肤可在此放置自定义入口或装饰 |

## 7 它刻意不做什么

- **不定义包格式**：摘要、签名、完整性、分发通道，留给 `dsh-ui-skin-manager` 的强契约体系（ADR 0001）与发行侧；
- **不规定故障隔离细则**：只规定"彻底关闭"语义与加载器如实报告的义务；事务、回滚代数、effect 账本属于强契约实现者的自由；
- **不约束皮肤内部**：皮肤用 theme token、槽位注入还是自有渲染层，公约一律不管；
- **不做宿主特化**：EAC 桌面宿主的槽位拓扑与能力边界由 `DSH-Desktop-EAC-UI-Skin-Authoring-Convention` 规定；它是本公约之下的一种宿主实现约定，两者不重叠、不竞争；
- **不取代默认观感**：`default`（无皮肤）永远是一个合法、可持久的选择。

## 8 版本与兼容

- 公约版本只随 §3/§4/§5 的语义破坏升 major；新增可选字段、新增 §6 槽位不升版本；
- 加载器可同时支持多个 apiVersion major；皮肤按自身声明的 major 被匹配；
- 加载器忽略未知字段；皮肤忽略未知槽位。双向宽容，互不升级绑架。

## 9 最小示例与自检表

最小皮肤（伪码，标准 DSH 插件 client 半）：

```ts
// package.json: { "dsh": { "skin": { "apiVersion": "dsh.ecosystem.ui-skin-loader/v1",
//   "id": "example.minimal", "name": "最小皮肤", "version": "0.1.0" }, "client": {...} } }
export default (ctx) => {
  const loader = ctx.inject(["uiSkinLoader"]);
  ctx.effect(() => {
    const off = loader.registerSkin({
      id: "example.minimal",
      activate: () => { /* 自此才产生副作用；全部经 ctx.effect 登记 */ },
      deactivate: () => { /* 撤销一切；退出后不可观测 */ },
    });
    return off; // fiber dispose 时自动反登记
  });
};
```

发布前自检（每题必须答"是"）：

- [ ] 未激活时，我的插件除了登记元数据什么都不做？（R1）
- [ ] `deactivate` 之后，宿主观感与我从未激活时逐像素一致？（§4.3）
- [ ] 我没有碰加载器保留面、其他皮肤、宿主恢复面？（R2/R3/R4）
- [ ] 我没有依赖任何 CSS-module hash / 私有 DOM / HMR 内部？（R5）
- [ ] 我不会自己记住"激活"状态、不会自启？（R6）
- [ ] 我没有启停或改写任何其他插件？（R7）

## 10 相关文档

- 生态索引：`dsh-ecosystem-spec`（本公约挂载于 `vendor/sub-protocols/dsh-skin`，索引条目见 `registry/protocols.json`）
- 强契约管理器体系：`DSH-EAC/dsh-ui-skin-manager`（ADR 0001/0002/0003；包格式、事务、故障隔离）
- 宿主特化创作约定：`DSH-EAC/DSH-Desktop-EAC-UI-Skin-Authoring-Convention`（EAC 桌面宿主）
- 参考实现（加载器与示例皮肤）：`DSH-EAC/dsh-ui-skin-loader`
